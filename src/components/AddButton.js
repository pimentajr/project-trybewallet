import React from 'react';

const AddButton = () => {

  const handleClick = () => {

  }

  return (
    <button type="submit" onClick={ handleClick } >
      Adicionar despesa
    </button>
  )
}

export default AddButton
